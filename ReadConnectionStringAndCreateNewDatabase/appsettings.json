{
  /**
   * ConnectionStrings section - used to store the connection strings
   * MainConnection - the main connection string
   * SecondaryConnection - the secondary connection string
   */
  "ConnectionStrings": {
    "MainConnection": "Data Source=.\\SQLEXPRESS;Initial Catalog=CustomerDatabase;Integrated Security=True;Encrypt=False",
    "SecondaryConnection": ""
  },
  /**
   * EntityConfiguration section - used to configure the Entity Framework Core or Dapper
   * If true, the application will create a new database
   * If false, the application will use the existing database
   */
  "EntityConfiguration": {
    "CreateNew": false
  }
}

